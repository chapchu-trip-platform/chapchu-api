apiVersion: v1
kind: ConfigMap
metadata:
  name: chapchu-api-config
  namespace: chapchu
data:
  # FE 프로덕션 URL 확정 시 쉼표로 추가 (예: "http://localhost:3000,https://chapchu.site")
  CORS_ALLOWED_ORIGINS: "http://localhost:3000"
